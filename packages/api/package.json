{
  "name": "@focus-reader/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@focus-reader/shared": "workspace:*",
    "@focus-reader/db": "workspace:*",
    "@focus-reader/parser": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8",
    "typescript": "^5",
    "vitest": "~3.2.0"
  }
}
